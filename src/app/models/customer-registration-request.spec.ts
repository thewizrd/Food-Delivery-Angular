import { CustomerRegistrationRequest } from './customer-registration-request';

describe('CustomerRegistrationRequest', () => {
  it('should create an instance', () => {
    expect(new CustomerRegistrationRequest()).toBeTruthy();
  });
});
