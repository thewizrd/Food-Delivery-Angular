import { CustomerUpdateRequest } from './customer-update-request';

describe('CustomerUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new CustomerUpdateRequest()).toBeTruthy();
  });
});
